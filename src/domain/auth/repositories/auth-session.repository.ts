// src/domain/auth/repositories/auth-session.repository.ts
// This file is not needed for stateless JWT authentication
// We don't store sessions in the database
export interface IAuthSessionRepository {
  // This is a placeholder for future use if needed
}